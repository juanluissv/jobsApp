export enum UserRole {
  ADMIN = 'admin',
  POSTULANT = 'postulant',
}
export enum JobStatus {
  OPEN = 'open',
  PAUSED = 'paused',
  CLOSED = 'closed',
}
export enum JobCondition {
  VOLUNTEER = 'volunteer',
  CONTRACT = 'contract',
}
export enum UserJobsStatus {
  ACCEPTED = 'accepted',
  PENDING = 'pending',
  REJECTED = 'rejected',
}
export enum Presency {
  FULL_PRESENCY = 'full_presency',
  SEMI_PRESENCY = 'semi_presency',
  REMOTE = 'remote',
}
